A simple NodeJS and ExpressJS front to back web application. 

Requirements - 
NodeJS and MongoDB

Run the below commands in the root folder (where package.json resides)
npm init
npm install (all dependencies in the package.json file will be installed locally)
node app.js (or node app) (runs the app) 

Open a browser and go to http://localhost:3000/ to view the web app. 